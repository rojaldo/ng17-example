import { FilterByHeightPipe } from './filter-by-height.pipe';

describe('FilterByHeightPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByHeightPipe();
    expect(pipe).toBeTruthy();
  });
});
